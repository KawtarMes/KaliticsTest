{% extends 'base.html.twig' %}

{% block body %}
	<div class="col-md-6">
		{# {{ readmeContent|markdown_to_html }} #}

		{# si on est connectés on pointe  #}
			{% if app.user %}
			<p>Pointage</p>
			<a href="{{ path('clocking_new')}}" class="btn btn-success">Je pointe</a>
	
			{# faire condition sur le role  #}


			{% else %}
				<a href="{{ path('login')}}" class="btn btn-primary">Connectez vous</a>

				<p>Vous avez pas de compte collaborateur inscrivez vous
					<a href="{{ path('sign_up') }}" class="btn btn-success">Créez un compte</a>
				</p>

		{% endif %}


	{% endblock %}
